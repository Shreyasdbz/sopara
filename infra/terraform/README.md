# Terraform foundation

Work Package 0 pins and validates Terraform syntax only. It defines no provider, backend, resource, data source, or apply target. GCP definitions begin after the historical vertical slice passes and Work Package 6 is authorized.
